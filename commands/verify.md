---
description: "Phase 6: mechanical gates, independent verifier verdict, merge on pass. One verify at a time. Requires the task implemented."
argument-hint: "[T-id]"
---

<important>
Use the Skill tool to invoke the `spearhead-verify` skill, passing `$ARGUMENTS` and the rest of this message unchanged.
</important>
