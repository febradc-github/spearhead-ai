---
description: "Phase 6: mechanical gates, independent verifier verdict, merge on pass. One verify at a time. Requires the task implemented."
argument-hint: "[T-id]"
---

<important>
Use the Skill tool to invoke the skill at `skills/spearhead-verify/` (registered name `spearhead:spearhead-verify`), passing `$ARGUMENTS` and the rest of this message unchanged.
</important>
