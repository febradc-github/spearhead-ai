---
description: "Orchestrating entry point: reads the current state and drives the pipeline phase by phase, pausing at every hard gate."
argument-hint: "[problem: what/why, expected vs actual, constraints]"
---

<important>
Use the Skill tool to invoke the `spearhead-attack` skill, passing `$ARGUMENTS` and the rest of this message unchanged.
</important>
