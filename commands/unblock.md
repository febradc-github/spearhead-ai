---
description: "Recover a blocked or stale task (retry / reset / replan) or clear a stale verify lock (--lock)."
argument-hint: "[T-id | --lock]"
---

<important>
Use the Skill tool to invoke the `spearhead-unblock` skill, passing `$ARGUMENTS` and the rest of this message unchanged.
</important>
