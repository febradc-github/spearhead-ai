---
description: "Git master: full git lifecycle management — commits, branches, history rewriting, remotes, conflict resolution, and more."
argument-hint: "[instruction]"
---

<important>
Use the Skill tool to invoke the `spearhead-git-master` skill, passing `$ARGUMENTS` and the rest of this message unchanged.
</important>
