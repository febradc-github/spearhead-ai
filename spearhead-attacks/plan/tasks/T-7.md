## Goal

Rewrite README.md's "Second-brain knowledge base" section for the `guru`-agent architecture and add a CHANGELOG entry.

## Expected files

- `README.md`
- `CHANGELOG.md`

## Depends on

T-1, T-2, T-3, T-4, T-5, T-6

## Acceptance criteria

- `README.md`'s "Second-brain knowledge base" section describes the `guru`-agent mechanism (search → staleness cross-check → source fallback → document), not the MCP server/CLI-ranking mechanism; contains no reference to `mcp-server/`, an MCP `search` tool, embeddings, or CLI-subprocess ranking as the current mechanism.
- Documents the expanded note taxonomy (`code`/`decisions`/`design`/`architecture`), the `cssclasses` frontmatter field, the opt-in CSS snippet's location and how to enable it in Obsidian, and a recommended Graph View group-by-`type` color setup.
- Documents `/spearhead:obsidian-graph`'s preconditions (Obsidian installed, Advanced URI plugin installed) and behavior (opens Obsidian to the graph view; cannot verify Obsidian actually reached it, only that the OS-level open command ran).
- `CHANGELOG.md` gains a new top entry describing the MCP-server-to-`guru`-agent swap and the Obsidian-compatibility additions; no existing entry is edited or deleted.
- `git grep -il "mcp-server\|voyage\|SPEARHEAD_EMBEDDINGS\|SPEARHEAD_RANKING_CLI"` returns no result in `README.md`.

## Out of scope

- Any code file — this task is docs-only.
- ADR-009 (already written during the design phase; this task does not touch `spearhead-attacks/decisions/`).
- Bumping the plugin version / marketplace manifest version (handled at ship time, per this repo's existing convention).

## Verification commands

- `git grep -il "mcp-server\|voyage\|SPEARHEAD_EMBEDDINGS\|SPEARHEAD_RANKING_CLI" -- README.md` (expect no output)
- Manual read-through of the rewritten README section against T-1 through T-6's actual shipped behavior.
