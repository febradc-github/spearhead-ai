## Goal

Rework `hooks/remind.js`'s and `rules/RULES.md`'s "try the `spearhead-knowledge` search tool first" nudge to instead nudge dispatching the `guru` agent.

## Expected files

- `hooks/remind.js`
- `hooks/remind.test.js`
- `rules/RULES.md`

## Depends on

none

## Acceptance criteria

- `hooks/remind.js` no longer references an MCP `spearhead-knowledge` search tool by name.
- Its nudge text instead instructs: before reading source files to answer a question, dispatch the `guru` agent (via the Agent tool) to check the knowledge base first, falling back to source only if `guru` finds nothing.
- `rules/RULES.md`'s identical line (currently: "Before reading source files to answer a question, try the `spearhead-knowledge` search tool first.") gets the same rewording — reworded consistently with `remind.js`'s new wording, since `RULES.md` is the canonical, single copy of this rule per its own header ("This file is the ONLY copy of the five verbatim spearhead rules... If a skill's summary and this file ever disagree, this file wins").
- Nudge cadence/injection logic (when the nudge fires, the idle-window/session-scoping mechanics) is unchanged — only the message text changes.
- Tests updated to assert the new wording; no test still asserts the old "spearhead-knowledge search tool" phrasing.

## Out of scope

- `agents/guru.md` itself (T-1's scope) — this task only references dispatching it by name.
- Any change to `remind.js`'s other nudges (the brevity/temperature directive, rules-refresh cadence, etc.) — untouched, only the search-first nudge's wording changes.
- Any of `rules/RULES.md`'s other four verbatim rules (clarification gate, anti-reward-hacking checklist, parallelism rule) — untouched, only the search-first line changes.

## Verification commands

- `node hooks/remind.test.js`
