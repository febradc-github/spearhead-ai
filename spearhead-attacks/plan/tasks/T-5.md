## Goal

Delete `mcp-server/` in full and remove `mcpServers` from both plugin manifests.

## Expected files

- `mcp-server/**` (delete — by the time this task runs, `hash.js`/`hash.test.js` are already relocated by T-2, so this deletes everything remaining: `server.js`, `server.test.js`, `lib/rank.js`, `lib/rank.test.js`, `lib/pipeline.js`, `lib/pipeline.test.js`, `lib/watch.js`, `lib/watch.test.js`, `lib/index-store.js`, `lib/index-store.test.js`, `package.json`, `package-lock.json`)
- `.claude-plugin/plugin.json` (modify)
- `.kimi-plugin/plugin.json` (modify)
- `.gitignore` (single-line removal only — see amendment note below)

## Depends on

T-2

## Acceptance criteria

- `mcp-server/` no longer exists in the working tree at all.
- `.claude-plugin/plugin.json` contains no `mcpServers` key.
- `.kimi-plugin/plugin.json` contains no `mcpServers` key; its `hooks` array entries (for `knowledge-nudge.js`, `guard.js`, `validate-state.js`, `remind.js`) are otherwise unchanged.
- `git grep -il "mcp-server\|mcpServers"` returns no result anywhere in shipped code, **except**: `spearhead-attacks/` (the historical decision record), `CHANGELOG.md`, `README.md` (stale — T-7's scope to fix), and `lib/knowledge-frontmatter.js`/`lib/knowledge-frontmatter.test.js` (their header comments name `mcp-server/` as a historical consumer — out of scope for this task, untouched, matches the existing "Out of scope" list below).
- The full test suite (`find . -name "*.test.js" -not -path "*/node_modules/*" -not -path "./spearhead-attacks/worktrees/*" | xargs node --test`) passes with 0 failures — no surviving test file references anything under `mcp-server/`.
- **(Amendment, added after V-5.1's verify failure)** `.gitignore` has a stray `mcp-server/node_modules/` line, left over from before this deletion — a genuine gap, not assigned to any prior task's scope. This task removes that one line only; no other line in `.gitignore` changes.

## Out of scope

- `lib/hash.js`/`lib/hash.test.js` (T-2's job — already relocated before this task runs; this task must not attempt to re-create or further modify them).
- `agents/guru.md`, `hooks/remind.js`, `rules/RULES.md` — untouched by this task.
- `lib/knowledge-frontmatter.js`/`lib/knowledge-frontmatter.test.js` — their stale header comments naming `mcp-server/` as a consumer are explicitly excused by the amended acceptance criterion above; not touched by this task.
- `README.md`/`CHANGELOG.md` (T-7's scope) — its stale reference is likewise explicitly excused above.
- Any line in `.gitignore` other than the single stray `mcp-server/node_modules/` entry named in the amendment.

## Verification commands

- `find . -name "*.test.js" -not -path "*/node_modules/*" -not -path "./spearhead-attacks/worktrees/*" | xargs node --test`
