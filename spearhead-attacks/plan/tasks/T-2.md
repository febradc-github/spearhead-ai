## Goal

Relocate `mcp-server/lib/hash.js`'s `hashContent` to `lib/hash.js`, and update `hooks/knowledge-nudge.js`'s import accordingly.

## Expected files

- `lib/hash.js` (create)
- `lib/hash.test.js` (create)
- `mcp-server/lib/hash.js` (delete)
- `mcp-server/lib/hash.test.js` (delete)
- `hooks/knowledge-nudge.js` (modify import path only)
- `hooks/knowledge-nudge.test.js` (modify import path only, if it references the old path)
- `mcp-server/lib/pipeline.js` (single-line import-path fix only — see amendment note below)
- `mcp-server/lib/pipeline.test.js` (single-line import-path fix only — see second amendment note below)

## Depends on

none

## Acceptance criteria

- `lib/hash.js` exports `hashContent` with identical behavior/signature to the current `mcp-server/lib/hash.js` — a pure relocation, no logic change.
- `lib/hash.test.js` is the relocated `mcp-server/lib/hash.test.js`, passing unchanged (path-relative requires updated as needed).
- `hooks/knowledge-nudge.js`'s import of `hashContent` points at `../lib/hash.js` (or the correct relative path from `hooks/`), not `../mcp-server/lib/hash.js`.
- `mcp-server/lib/hash.js` and `mcp-server/lib/hash.test.js` no longer exist.
- No file outside this task's expected set imports from `mcp-server/lib/hash.js` after this task (confirm via `git grep`).
- **(Amendment, added after V-2.1's mechanical gate failure)** `mcp-server/lib/pipeline.js:27` has `const { hashContent } = require('./hash.js');` — a same-directory require against `mcp-server/lib/hash.js`, which this task correctly relocates away. This broke `mcp-server/lib/pipeline.test.js` and (transitively, via `server.js` requiring `pipeline.js`) `mcp-server/server.test.js` with `MODULE_NOT_FOUND`. This task updates ONLY that one require line in `pipeline.js` to point at the new `../../lib/hash.js` location (or the correct relative path from `mcp-server/lib/`) — a narrow, temporary bridge fix, since T-5 deletes `pipeline.js` entirely within this same attack shortly after. No other part of `pipeline.js` changes.
- **(Second amendment, retroactively formalizing a fix already applied during the repair pass)** `mcp-server/lib/pipeline.test.js` itself also directly required the old `./hash.js` sibling path (independent of `pipeline.js`'s own import) — without an identical one-line fix there, `node mcp-server/lib/pipeline.test.js` still threw `MODULE_NOT_FOUND` even after `pipeline.js` was corrected, since the test file's own import failed before `pipeline.js`'s logic ever ran. Same treatment as above: import path only, zero change to any test name, assertion, or count (confirmed 9/9, identical to the pre-relocation baseline).

## Out of scope

- Deleting the rest of `mcp-server/` (T-5's job — this task only removes `hash.js`/`hash.test.js` specifically, since they must be relocated, not just deleted; and fixes the one broken import in `pipeline.js` per the amendment above).
- Any change to `hooks/knowledge-nudge.js`'s staleness-check logic itself (the three-way `new`/`current`/`stale` state derivation) — only the import path changes.
- Any change to `mcp-server/lib/pipeline.js` beyond the single `require` line named in the amendment above — its own logic, tests, and everything else about it stay exactly as A-4 shipped it.
- `mcp-server/server.js`/`server.test.js` — not touched directly; they only benefit transitively once `pipeline.js`'s import is fixed.
- `agents/guru.md` (T-1's scope) — this task doesn't write it, just makes `lib/hash.js` exist at the location `guru.md` references.

## Verification commands

- `node lib/hash.test.js`
- `node hooks/knowledge-nudge.test.js`
- `node mcp-server/lib/pipeline.test.js` (added by this amendment — confirms the import fix; no other assertion in this file should need to change)
- `node mcp-server/server.test.js` (added by this amendment — confirms the transitive fix; should already pass unchanged once `pipeline.js` loads correctly)
