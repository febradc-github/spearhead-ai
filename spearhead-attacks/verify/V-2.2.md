# VERIFY — T-2 (attempt 2): relocate hash.js to lib/ + pipeline.js bridge fix

## Mechanical gates

**Command:** `find . -name "*.test.js" -not -path "*/node_modules/*" | xargs node --test` (full suite, run on `spearhead/T-2`'s worktree after both amendment/repair commits).

**Result: PASS.** 195/195 pass, 0 fail. Both `pipeline.test.js` and `server.test.js` (which failed at V-2.1 with `MODULE_NOT_FOUND`) now load and pass cleanly.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| `lib/hash.js` byte-identical relocation of `hashContent`, no logic change | pass |
| `lib/hash.test.js` byte-identical relocation | pass |
| `hooks/knowledge-nudge.js` imports from `../lib/hash.js`, not the old path | pass |
| Old `mcp-server/lib/hash.js`/`hash.test.js` no longer exist | pass |
| No stray import of the old path anywhere (`git grep` swept) | pass |
| (Amendment 1) `pipeline.js`'s require fixed — single line, nothing else changed | pass |
| (Amendment 2) `pipeline.test.js`'s require fixed — single line, 9/9 tests byte-identical to baseline | pass |
| `server.test.js` passes transitively (8/8) | pass |

### Anti-reward-hacking checklist

- (a) no test deleted/skipped without justification: pass — `hash.test.js` relocated with all 5 tests intact; `pipeline.test.js`'s 9 tests, `knowledge-nudge.test.js`'s 21, `server.test.js`'s 8 all unchanged, `skipped: 0` throughout.
- (b) no assertion weakened vs testing strategy: pass — verifier confirmed `pipeline.test.js`'s diff is exactly one import line, every test name/assertion/count byte-identical to the pre-relocation baseline — distinguishing a genuine narrow bridge fix from a repair pass that also touched test logic.
- (c) no hardcoded expected output: pass — no fixture/expected-value edits anywhere in the diff, only `require` paths and file relocation.
- (d) diff touches nothing out-of-scope: pass — confined to exactly the 8 expected files; `server.js`, `guru.md`, `watch.js`, `index-store.js`, embeddings/similarity files all untouched; `knowledge-nudge.js`'s staleness-check logic itself unchanged.

**Overall: pass.**

## Merge and integration check

- Local uncommitted `spearhead-attacks/` edits (A-5's in-progress decision-record files) stashed before merge, restored after — same reason as T-1's merge.
- `git merge --no-ff spearhead/T-2` into `main` — clean merge, `hash.js`/`hash.test.js` renamed, `knowledge-nudge.js`/`.test.js` and `pipeline.js`/`.test.js` each gained a one-line import fix.
- Full suite re-run on merged tree: 195/195 pass, 0 fail.
- **Integration: pass.**
