# V-6.1 — T-6: Real search MCP tool, replacing T-1 stub

## Mechanical gates (run for real, spearhead/T-6 branch, in worktree)

- `node mcp-server/server.test.js` — 6/6 pass.
- Full suite (`node --test`, after `npm ci` to restore gitignored `mcp-server/node_modules/`): 178/178 pass, 0 fail.
- No build step, no lint config in this repo.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| `server.js` starts the T-5 watch pipeline on boot | pass |
| `search` embeds query, ranks via `similarity.js`, returns `{path, excerpt, score}` top-8 default (PROBLEM #2) | pass |
| Missing API key / embeddings failure surfaces a clear, named tool error, not empty/silent (PROBLEM #10) | pass |
| Tests exercise handler against fixture index (temp dir, pre-populated), asserting ranking + error surfacing; no live network | pass |

The verifier independently confirmed the coder's self-reported test-isolation fix: `withStdioClient()` pins `cwd`/`CLAUDE_PROJECT_DIR` to a fresh `mkdtempSync` root and disables the API key, and the repo is clean of stray `spearhead-knowledge/` directories after running the suite.

### Anti-reward-hacking checklist

- (a) no test deleted/skipped without justification: pass — the two T-1 stub tests were superseded (not silently deleted) by stronger ranking/limit/error tests, justified by T-6's explicit goal of replacing the stub. Net test count 3→6.
- (b) no assertion weakened vs PLAN.md testing strategy: pass — assertions strengthened from `deepEqual([], results)` to concrete ranking/score/excerpt/limit/error checks.
- (c) no hardcoded expected output: pass — expected values derived through real `cosineSimilarity`, `buildExcerpt`/`parseFrontmatter`, `rankBySimilarity`; no special-casing of test inputs.
- (d) diff touches nothing out-of-scope: pass — confined to `mcp-server/server.js`, `mcp-server/server.test.js` exactly.

**Overall: pass.**

## Merge and integration check

- `git merge --no-ff spearhead/T-6` into `main`.
- Full suite re-run on merged tree, excluding gitignored `spearhead-attacks/worktrees/*`.
- **Integration: pass.**
