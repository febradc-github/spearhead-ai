# V-1.1 — T-1: MCP server skeleton + dual-runtime manifest declaration

## Mechanical gates (run for real, spearhead/T-1 branch, in worktree)

- `node mcp-server/server.test.js` — 3/3 pass.
- Full pre-existing suite (unaffected):
  - `hooks/remind.test.js` — 14/14 pass.
  - `hooks/validate-state.test.js` — 9/9 pass.
  - `hooks/guard.test.js` — 14/14 pass.
  - `scripts/state.test.js` — 34/34 pass.
- Total: 74/74 pass, 0 fail.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| `server.js` stdio MCP server, one `search` tool, schema `{query, limit?}`, stub `{results: []}` | pass |
| `package.json` sole dep `@modelcontextprotocol/sdk`; `package-lock.json` real (`npm install` reproduces with zero diff) | pass |
| Both manifests gain `mcpServers` block pointing at `mcp-server/server.js`, matching each runtime's existing referencing style | pass |
| `node mcp-server/server.test.js` passes (start, list tools, schema assert, stub call assert) | pass |

### Anti-reward-hacking checklist

- (a) no test deleted/skipped: pass — all files additions, 0 skipped/todo.
- (b) no assertion weakened vs PLAN.md testing strategy: pass — `node:test`/`node:assert/strict`, direct run, no live network, real client/server round-trip.
- (c) no hardcoded expected output: pass — stub `{results: []}` is the deliberate, documented contract for this task; handler and test derive it independently.
- (d) diff touches nothing out-of-scope: pass — no `scripts/knowledge-path.js`, no real search/embeddings/index logic (only comments). `.gitignore` addition (`mcp-server/node_modules/`) is in-scope support.

**Overall: pass.**

## Merge and integration check

- `git merge --no-ff spearhead/T-1` into `main` — commit `13238d9`.
- `mcp-server/` deps not tracked (gitignored `node_modules/`); ran `npm install` on merged tree — reproduces lockfile with zero diff, 93 packages.
- Full suite re-run on merged tree: 74/74 pass (remind 14, validate-state 9, guard 14, state 34, mcp-server 3).
- **Integration: pass.**

## Follow-up for retro

- `npm audit` on the merged tree: 2 moderate severity transitive vulnerabilities (in `@hono/node-server`, pulled in by the MCP SDK's optional HTTP/SSE transport, unused by this stdio-only server). Fixing requires `npm audit fix --force`, which downgrades the SDK — out of scope for T-1.
