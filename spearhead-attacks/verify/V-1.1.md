# VERIFY — T-1 (attempt 1): agents/guru.md search+staleness+fallback+document sub-agent

## Mechanical gates

**Command:** `find . -name "*.test.js" -not -path "*/node_modules/*" | xargs node --test` (full suite, run on `spearhead/T-1`'s worktree, `mcp-server/` dependencies via `npm ci`).

**Result: PASS.** 195/195 pass, 0 fail (no code changed — a new prose file, no automated test).

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| Follows `spearhead-scout.md`'s frontmatter/structure pattern (`name`/`description`/`model`/`effort`, isolation justification, numbered constraints, process/reporting) | pass |
| `description` states internal-dispatch-only, "never invoke directly," no user-facing command | pass |
| Documented 5-step process (search → staleness cross-check → source fallback → document → grounded answer) matches every specified detail | pass |
| Explicitly scopes writes to `code/` notes only; `decisions`/`design`/`architecture` stay agent-judgment since A-1 | pass |
| Never fabricates; reports plainly if fallback also finds nothing | pass |
| Documents kimi-code fallback (inline, no dispatch mechanism) — verifier confirmed `.kimi-plugin/plugin.json` has no `agents:` field | pass |
| References `lib/hash.js` (relocated location), not `mcp-server/lib/hash.js` | pass |

### Anti-reward-hacking checklist

- (a)/(b)/(c) not applicable — no test exists for or is touched by this task; PLAN.md's testing strategy explicitly designates this as manual structural review.
- (d) diff touches nothing out-of-scope: pass — confined to the single new `agents/guru.md` file; `hooks/knowledge-nudge.js`, `hooks/remind.js`, `scripts/knowledge-path.js`, `lib/knowledge-frontmatter.js` all untouched.

**Overall: pass.** Verifier independently confirmed every cross-reference in the file (`hooks/knowledge-nudge.js`'s comparison direction, `ADR-004`'s naming authority, `lib/knowledge-frontmatter.js`'s frontmatter grammar) is real and consistent — content genuinely satisfies every criterion, not just superficially.

## Merge and integration check

- Local uncommitted `spearhead-attacks/` edits (A-5's in-progress decision-record files) stashed before merge, restored after — same reason as every prior attack's merges this session (branch cut before this attack's decision-record files existed in git history).
- `git merge --no-ff spearhead/T-1` into `main` — clean merge, only `agents/guru.md` created.
- Full suite re-run on merged tree: 195/195 pass, 0 fail.
- **Integration: pass.**
