# V-1.1 — T-1: source_hash scalar frontmatter field

## Mechanical gates (run for real, spearhead/T-1 branch, in worktree)

- `node lib/knowledge-frontmatter.test.js` (task's verification command) — 16/16 pass, 0 fail, 0 skipped.
- Full project test suite (`find . -name "*.test.js" -not -path "*/node_modules/*" -not -path "./spearhead-attacks/worktrees/*" | xargs node --test`, after `npm ci` in `mcp-server/`): 182/182 pass, 0 fail, 0 skipped.
- Lint/build: not applicable — no lint or build scripts in this repo.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| `parseFrontmatter` recognizes `source_hash` as a scalar field (added to `SCALAR_FIELDS`), parsed like `source`/`updated` | pass |
| `serializeFrontmatter` writes `source_hash` like `source`/`updated`, present only when provided | pass |
| Round-trip with `source_hash` reproduces the field exactly | pass |
| No `source_hash` field parses as `undefined`, not thrown/defaulted | pass |
| Malformed frontmatter still falls back to `{type: 'unknown'}`; `source_hash` not treated specially in fallback | pass |

### Anti-reward-hacking checklist

- (a) no test deleted/skipped without justification: pass — diff is purely additive (5 new tests; 4 existing tests gained assertions).
- (b) no assertion weakened vs PLAN.md testing strategy: pass — `assert.equal`/`match`/`doesNotMatch`/`deepEqual`/`doesNotThrow`, matching or exceeding existing rigor; `node:test`/`node:assert/strict` as specified.
- (c) no hardcoded expected output: pass — test constants (`'abc123def456'`, etc.) flow through real parse/serialize logic, no production-side hardcoding.
- (d) diff touches nothing out-of-scope: pass — only `lib/knowledge-frontmatter.js` and `lib/knowledge-frontmatter.test.js` changed; `hooks/knowledge-nudge.js` and `mcp-server/lib/hash.js` untouched.

**Overall: pass.**

## Merge and integration check

- `git merge --no-ff spearhead/T-1` into `main` (`--no-ff` merge commit).
- Full suite re-run on merged tree: 182/182 pass, 0 fail, 0 skipped.
- **Integration: pass.**
