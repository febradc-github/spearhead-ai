# VERIFY — T-4 (attempt 1): rework remind.js and RULES.md search-first nudge to dispatch guru

## Mechanical gates

**Command:** `find . -name "*.test.js" -not -path "*/node_modules/*" | xargs node --test` (full suite, run on `spearhead/T-4`'s worktree, `mcp-server/` dependencies via `npm ci`).

**Result: PASS.** 195/195 pass, 0 fail (no test count change — same 15 tests, wording updated in place).

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| `hooks/remind.js` no longer references an MCP `spearhead-knowledge` search tool by name | pass |
| Nudge text instructs dispatching `guru` first, source fallback only if `guru` finds nothing | pass |
| `rules/RULES.md`'s identical line reworded consistently with `remind.js` | pass |
| Nudge cadence/injection logic unchanged — only message text changed | pass |
| Tests updated to assert new wording; no test still asserts old phrasing | pass |

### Anti-reward-hacking checklist

- (a) no test deleted/skipped without justification: pass — test count unchanged (15/15), search-first test updated in place, no removal.
- (b) no assertion weakened vs testing strategy: pass — assertions strengthened, not loosened: new positive matchers for the guru wording plus two new `assert.doesNotMatch` guards against the old "spearhead-knowledge search tool" phrasing in both `fullMessage()` and `anchor()`.
- (c) no hardcoded expected output: pass — assertions are regex matches against dynamically produced output; `fullMessage()` reads `RULES.md` from disk at runtime, forcing the real file to carry the correct wording.
- (d) diff touches nothing out-of-scope: pass — confined to exactly the three expected files; `agents/guru.md`, `remind.js`'s other nudges, and `RULES.md`'s other four verbatim rules all verified byte-identical/untouched.

**Overall: pass.**

## Merge and integration check

- Local uncommitted `spearhead-attacks/` edits (A-5's in-progress decision-record files) stashed before merge, restored after — same reason as T-1/T-2/T-3's merges.
- `git merge --no-ff spearhead/T-4` into `main` — clean merge, only `hooks/remind.js`, `hooks/remind.test.js`, `rules/RULES.md` changed.
- Full suite re-run on merged tree: 200/200 pass, 0 fail.
- **Integration: pass.**
