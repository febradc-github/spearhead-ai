# V-5.1 — T-5: File-watch pipeline wiring hash + embeddings + index store

## Mechanical gates (run for real, spearhead/T-5 branch, in worktree)

- `node mcp-server/lib/watch.test.js` — 18/18 pass.
- `node mcp-server/lib/pipeline.test.js` — 11/11 pass.
- Full suite (`node --test`, after `npm ci` to restore gitignored `mcp-server/node_modules/` in this fresh worktree): 160/160 pass, 0 fail.
- No build step, no lint config in this repo (confirmed against CONTEXT.md).

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| `watch.js` recursively watches `spearhead-knowledge/**/*.md`, `spearhead-attacks/**/*.md`, and general-doc paths (`README.md`, `docs/**/*.md`) | pass |
| On create/change: hash computed; skip if matches existing entry, else embed + update index (PROBLEM #1, #3) | pass |
| Events processed sequentially via in-memory queue, not concurrent embeddings calls (DESIGN load-spike) | pass |
| On embeddings failure, entry marked `pending`, retries next event, watcher does not crash | pass |
| On startup, re-scans and reconciles via the same hash-comparison path | pass |
| Tests use temp dir and injected/mocked embeddings client; no live network | pass |

### Anti-reward-hacking checklist

- (a) no test deleted/skipped: pass — all additions, 0 skipped.
- (b) no assertion weakened vs PLAN.md testing strategy: pass — substantive assertions (`maxInFlight===1`, exact `calls.length`, `pending` transitions, `deepEqual` index contents).
- (c) no hardcoded expected output: pass — expected embeddings/hashes derived from real inputs, not literal magic values.
- (d) diff touches nothing out-of-scope: pass — confined to the four expected files (`mcp-server/lib/watch.js`, `watch.test.js`, `pipeline.js`, `pipeline.test.js`).

**Overall: pass.**

## Merge and integration check

- `git merge --no-ff spearhead/T-5` into `main` (merge commit, no-ff).
- Full suite re-run on merged tree, excluding gitignored `spearhead-attacks/worktrees/*` (in-flight parallel worktrees on disk, not part of `main`'s tree): 160/160 pass, 0 fail.
- **Integration: pass.**
