# VERIFY — T-5 (attempt 1): delete mcp-server and manifest mcpServers wiring

## Mechanical gates

**Command:** `find . -name "*.test.js" -not -path "*/node_modules/*" | xargs node --test` (full suite, run on `spearhead/T-5`'s worktree).

**Result: PASS.** 137/137 pass, 0 fail.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| `mcp-server/` no longer exists in the working tree at all | pass |
| `.claude-plugin/plugin.json` contains no `mcpServers` key | pass |
| `.kimi-plugin/plugin.json` contains no `mcpServers` key; `hooks` array unchanged | pass |
| `git grep -il "mcp-server\|mcpServers"` returns no result outside `spearhead-attacks/`/`CHANGELOG.md` | **fail** |
| Full test suite passes, 0 failures | pass |

### Criterion 4 failure detail

Four references survive on the branch: `README.md:202`, `lib/knowledge-frontmatter.js:3`, `lib/knowledge-frontmatter.test.js:3` (all three explicitly named in this task's own "Out of scope" section — an inconsistency in how the task file was written, since the acceptance criterion's wording never whitelisted them), and `.gitignore:3` (`mcp-server/node_modules/`) — a genuine gap, assigned to neither this task's expected-files nor its out-of-scope list.

### Anti-reward-hacking checklist

- (a) no test deleted/skipped without justification: pass — five whole `.test.js` files deleted, exactly matching the task's explicit purpose (deleting the directory they exercised); no test outside `mcp-server/` touched.
- (b) no assertion weakened vs testing strategy: pass — zero test files outside `mcp-server/` touched by the diff.
- (c) not applicable — pure deletion, no new logic.
- (d) diff touches nothing out-of-scope: pass — confined to exactly the three expected paths.

**Overall: fail (criterion 4 only).** The deletion itself, manifest edits, kimi hooks integrity, and full suite are all correct — but the acceptance criterion as literally written is not satisfied, and a `.gitignore` gap exists that no task currently owns.

## Verdict

**Gate: verdict fail on one criterion.** Per the verify skill: write this report, transition T-5 back to `in_progress`, unlock, report. This is a task-file authoring gap (an acceptance criterion inconsistent with its own out-of-scope section, plus an unassigned `.gitignore` cleanup), not an implementation bug — needs a plan decision (`/spearhead:replan`) to (1) fix criterion 4's wording to explicitly whitelist the already-documented out-of-scope files, and (2) add `.gitignore` to T-5's expected files for the one-line stray-entry removal, before a repair pass.

## State transitions

- `state.js transition T-5 in_progress`
- `state.js unlock`
