# V-7.1 — T-7: Deterministic naming script (scripts/knowledge-path.js)

## Mechanical gates (run for real, spearhead/T-7 branch, in worktree)

- `node scripts/knowledge-path.test.js` — 8/8 pass.
- Full pre-existing suite (unaffected):
  - `hooks/remind.test.js` — 15/15 pass.
  - `hooks/validate-state.test.js` — 9/9 pass.
  - `hooks/guard.test.js` — 14/14 pass.
  - `scripts/state.test.js` — 34/34 pass.
  - `lib/knowledge-frontmatter.test.js` — 12/12 pass.
  - `mcp-server/server.test.js` — 3/3 pass.
- Total: 95/95 pass, 0 fail.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| Prints `spearhead-knowledge/code/<parent>-<basename>.md` | pass |
| Different-source collision escalates one more parent level; existing notes never renamed | pass |
| Same-source collision returns the same path (refresh) | pass |
| Creates `spearhead-knowledge/code/` if missing | pass |
| Tests use temp dir with fixture notes for all three cases | pass |
| Genuinely uses `lib/knowledge-frontmatter.js`'s `parseFrontmatter`, not reinvented | pass |

### Anti-reward-hacking checklist

- (a) no test deleted/skipped: pass — net-new files, 0 skipped/todo.
- (b) no assertion weakened vs PLAN.md testing strategy: pass — exact-equality assertions including CLI stdout.
- (c) no hardcoded expected output: pass — slug derived algorithmically; collision detection depends on genuine parsed frontmatter.
- (d) diff touches nothing out-of-scope: pass — confined to the two expected files; no MCP logic, no doc-content writing.

Double-collision (recursive escalation) explicitly verified by a dedicated test.

**Overall: pass.**

## Merge and integration check

- `git merge --no-ff spearhead/T-7` into `main`.
- Full suite re-run on merged tree: 131/131 pass (remind 15, validate-state 9, guard 14, state 34, knowledge-frontmatter 12, mcp-server/server 3, hash 5, embeddings 9, index-store 10, similarity 12, knowledge-path 8).
- **Integration: pass.**
