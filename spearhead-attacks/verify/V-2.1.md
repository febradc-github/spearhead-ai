# V-2.1 — T-2: Shared frontmatter parser

## Mechanical gates (run for real, spearhead/T-2 branch, in worktree)

- `node lib/knowledge-frontmatter.test.js` — 12/12 pass.
- Full pre-existing suite (unaffected):
  - `hooks/remind.test.js` — 14/14 pass.
  - `hooks/validate-state.test.js` — 9/9 pass.
  - `hooks/guard.test.js` — 14/14 pass.
  - `scripts/state.test.js` — 34/34 pass.
- Total: 83/83 pass, 0 fail.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| Exports `parseFrontmatter`/`serializeFrontmatter` handling `type`, `tags`, `related`, `source`, `updated` | pass |
| `tags`/`related` use YAML block-list style, not flow style | pass |
| Malformed/missing frontmatter falls back to `{type: 'unknown'}` without throwing | pass |
| Round-trip `parse(serialize(x))` reproduces fields and body | pass |
| Dependency-free (Node built-ins only) | pass |

### Anti-reward-hacking checklist

- (a) no test deleted/skipped: pass — net-new file, 0 skipped/todo.
- (b) no assertion weakened vs PLAN.md testing strategy: pass — `node:test`/`node:assert/strict`, strict assertions throughout.
- (c) no hardcoded expected output: pass — genuine parser/serializer; round-trip composition can't be satisfied by hardcoding.
- (d) diff touches nothing out-of-scope: pass — diff confined to exactly the two expected files.

**Overall: pass.**

## Merge and integration check

- `git merge --no-ff spearhead/T-2` into `main`.
- Full suite re-run on merged tree: 86/86 pass (remind 14, validate-state 9, guard 14, state 34, mcp-server 3, knowledge-frontmatter 12).
- **Integration: pass.**
