# VERIFY — T-2 (attempt 1): relocate hash.js to lib/

## Mechanical gates

**Command:** `find . -name "*.test.js" -not -path "*/node_modules/*" | xargs node --test` (full suite, run on `spearhead/T-2`'s worktree, `mcp-server/` dependencies via `npm ci`).

**Result: FAIL.** 178/180 pass, 2 fail — both are whole-file load failures, not assertion failures, in files T-2 does not touch:

```
✖ mcp-server/lib/pipeline.test.js — 'test failed'
✖ mcp-server/server.test.js — 'test failed'
```

Direct cause (confirmed by running `node mcp-server/lib/pipeline.test.js` standalone):

```
Error: Cannot find module './hash.js'
Require stack:
- mcp-server/lib/pipeline.js
- mcp-server/lib/pipeline.test.js
code: 'MODULE_NOT_FOUND'
```

## Root cause

Real, direct consequence of T-2's own correct change, not an unrelated
flake or a T-2 implementation bug (T-2's own `lib/hash.test.js` is 5/5
green, `hooks/knowledge-nudge.test.js` is 21/21 green). `mcp-server/lib/pipeline.js:27`
has `const { hashContent } = require('./hash.js');` — a same-directory
require against `mcp-server/lib/hash.js`, which T-2 correctly relocated
away (to `lib/hash.js`) per its own acceptance criteria. `pipeline.js`
is explicitly out of scope for T-2 (a later task, T-5, deletes it
entirely along with the rest of `mcp-server/`) — T-2 had no mandate to
touch it, but deleting the file `pipeline.js` requires broke it anyway.
`server.test.js` fails transitively, since `server.js` requires
`pipeline.js`.

This is the same recurring pattern seen in A-3's and A-4's verify
attempts: a task's own approved, correct change breaks a pre-existing
file that's explicitly out of scope for it, surfacing as a genuine
full-suite mechanical-gate failure. Unlike those prior cases, this one
resolves itself almost immediately — T-5 (which depends on T-2 for
exactly this reason) deletes `pipeline.js` outright within the same
attack, so a narrow, temporary one-line import-path fix in `pipeline.js`
now is the pragmatic bridge, not a lasting change to a file T-2 doesn't
otherwise own.

## Verdict

**Gate fails. Stopping per the verify skill's hard rule** — the full
suite must pass for real before an independent verdict is assembled or
any merge happens. No independent verifier dispatch, no merge, in this
attempt.

This is a task-boundary/plan gap. T-2's out-of-scope list forbids
touching `mcp-server/lib/pipeline.js`, but T-2's correct relocation of
`hash.js` breaks that file's only import of it. Needs a plan decision
(`/spearhead:replan`) before a repair attempt — the coder cannot fix a
failure in a file it's explicitly barred from touching.

## State transitions

- `state.js transition T-2 in_progress`
- `state.js unlock`
