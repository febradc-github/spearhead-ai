# V-2.1 — T-2: handleRead staleness detection + wikilink guidance

## Mechanical gates (run for real, spearhead/T-2 branch, in worktree)

- `node hooks/knowledge-nudge.test.js` (task's verification command) — 21/21 pass, 0 fail, 0 skipped.
- Full project test suite (`find . -name "*.test.js" -not -path "*/node_modules/*" -not -path "./spearhead-attacks/worktrees/*" | xargs node --test`, after `npm ci` in `mcp-server/`): 188/188 pass, 0 fail, 0 skipped.
- Lint/build: not applicable — no lint or build scripts in this repo.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| Computes source hash via `hashContent` (imported, no new logic); derives three-way state `new`/`current`/`stale` | pass |
| State `current` never nudges, regardless of session | pass |
| State `stale` nudges with refresh framing (names note path, in-place update + `## Changelog`, not duplicate) | pass |
| Both `new` and `stale` go through the session-throttle check before firing | pass |
| Throttle schema: array → path→hash map; changed file re-nudges; old-format array treated as empty, never crashes | pass |
| Both `new` and `stale` nudges instruct setting `source_hash` in frontmatter | pass |
| All three nudge sites (`handleRead` new-note, `handleRead` refresh, `handleBash` task-done) include the wikilink-discipline line | pass |
| `handleBash` detection logic unchanged, only message text gains the wikilink line | pass |
| Hash/frontmatter failures degrade gracefully (unreadable source → silent; malformed note → `source_hash` undefined → `stale`) | pass |
| Tests cover matching-hash silence, missing/mismatched refresh, same-(path,hash) throttle, changed-file re-nudge, wikilink presence in all three sites | pass |

### Anti-reward-hacking checklist

- (a) no test deleted/skipped without justification: pass — no tests removed; one existing test title updated to reflect new source_hash semantics with assertions preserved/strengthened; 7 net-new tests added, 0 skipped.
- (b) no assertion weakened vs PLAN.md testing strategy: pass — all required coverage categories present and asserted positively; existing assertions tightened (added `source_hash`, `[[wikilinks]]`, negative-phrasing checks), not loosened.
- (c) no hardcoded expected output: pass — expected paths/hashes derived at runtime via `computeKnowledgePath`/`hashContent`; mismatch test uses a deliberately-wrong constant hash, not a hardcoded match.
- (d) diff touches nothing out-of-scope: pass — only `hooks/knowledge-nudge.js` and `hooks/knowledge-nudge.test.js` changed; MCP server, index/search, `scripts/knowledge-path.js`, `remind.js` untouched.

**Overall: pass.**

## Merge and integration check

- `git merge --no-ff spearhead/T-2` into `main` (`--no-ff` merge commit).
- Full suite re-run on merged tree: 188/188 pass, 0 fail, 0 skipped.
- **Integration: pass.**
