# VERIFY — T-5 (attempt 2): delete mcp-server and manifest mcpServers wiring

## Mechanical gates

**Command:** `find . -name "*.test.js" -not -path "*/node_modules/*" | xargs node --test` (full suite, run on `spearhead/T-5`'s worktree after the amendment/repair commit).

**Result: PASS.** 137/137 pass, 0 fail.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| `mcp-server/` no longer exists in the working tree at all | pass |
| `.claude-plugin/plugin.json` contains no `mcpServers` key | pass |
| `.kimi-plugin/plugin.json` contains no `mcpServers` key; `hooks` array unchanged | pass |
| `git grep` sweep returns only whitelisted exceptions — `.gitignore` no longer among them, no new stray reference introduced | pass |
| Full test suite passes, 0 failures | pass |
| (Amendment) `.gitignore`'s stray line removed, nothing else in that file changed | pass |

### Anti-reward-hacking checklist

- (a) no test deleted/skipped without justification: pass — 6 `mcp-server/**` test files deleted, exactly the task's explicit goal; no other test touched.
- (b) no assertion weakened vs testing strategy: pass — zero test files outside `mcp-server/` touched.
- (c) not applicable — pure deletion, no new logic.
- (d) diff touches nothing out-of-scope: pass — confined to exactly the four expected paths; verifier independently confirmed no out-of-scope path (`lib/hash.*`, `agents/guru.md`, `hooks/remind.js`, `rules/RULES.md`, `lib/knowledge-frontmatter.*`, `README.md`, `CHANGELOG.md`) touched.

**Overall: pass.**

## Merge and integration check

- Local uncommitted `spearhead-attacks/` edits (A-5's in-progress decision-record files) stashed before merge, restored after — same reason as every prior merge this attack.
- `git merge --no-ff spearhead/T-5` into `main` — clean merge, 15 files changed (all deletions plus the two manifest/`.gitignore` edits).
- A stale, untracked `mcp-server/node_modules/` directory (leftover from an old `npm ci` run before this deletion, never committed) was found on disk after the merge and removed — filesystem hygiene, not a git-tracked change.
- Full suite re-run on merged tree: 137/137 pass, 0 fail.
- **Integration: pass.**
