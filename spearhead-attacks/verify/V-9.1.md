# V-9.1 — T-9: knowledge-nudge.js hook: code-doc-on-first-read + task-done doc update

## Mechanical gates (run for real, spearhead/T-9 branch, in worktree)

- `node hooks/knowledge-nudge.test.js` — 15/15 pass.
- Full suite (`node --test`, after `npm ci` to restore gitignored `mcp-server/node_modules/`): 146/146 pass, 0 fail.
- No build step, no lint config in this repo.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| Registered as `PostToolUse` in `hooks/hooks.json` and `.kimi-plugin/plugin.json` with matchers `Read` and `Bash\|PowerShell` | pass |
| On `Read` of an undocumented source file, inject nudge naming the exact `knowledge-path.js` target (PROBLEM #4) | pass |
| Re-reads of documented/unchanged file in same session don't re-nudge (session-scoped, idle-expiry like remind.js) (PROBLEM #6) | pass |
| On successful `state.js transition <T-id> done`, read task's expected files from `status.yml` (read-only), nudge `## Changelog` per file (PROBLEM #12) | pass |
| Never writes `status.yml`, never calls embeddings API; completes within 10s timeout (DESIGN ADR-003) | pass |
| Tests use `spawnSync` against fixture PostToolUse payloads for both matchers | pass |

This task's expected-file set was corrected via `/spearhead:replan` prior to this run: `.claude-plugin/plugin.json` (which has no `hooks` field in this repo) was swapped for `hooks/hooks.json` (this repo's actual Claude Code hook-registration file, confirmed against `remind.js`/`guard.js`/`validate-state.js` and the upstream sibling project). Verified against the corrected task file.

### Anti-reward-hacking checklist

- (a) no test deleted/skipped: pass — new test file only, 15/15 run and pass.
- (b) no assertion weakened vs PLAN.md testing strategy: pass — `node:test`/`spawnSync` fixture pattern, meaningful assertions (exact target path, `## Changelog`, task/attack IDs, silence on negatives).
- (c) no hardcoded expected output: pass — target paths derived via the real `computeKnowledgePath`; status.yml fixtures built through `state.js` CLI, not hand-fabricated.
- (d) diff touches nothing out-of-scope: pass — confined to `hooks/knowledge-nudge.js`, `hooks/knowledge-nudge.test.js`, `hooks/hooks.json`, `.kimi-plugin/plugin.json`.

**Overall: pass.**

## Merge and integration check

- `git merge --no-ff spearhead/T-9` into `main` (T-5 already merged).
- Full suite re-run on merged tree, excluding gitignored `spearhead-attacks/worktrees/*`.
- **Integration: pass.**
