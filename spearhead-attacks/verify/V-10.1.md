# V-10.1 — T-10: README + CHANGELOG documentation

## Mechanical gates (run for real, spearhead/T-10 branch, in worktree)

- `node -e "require('fs').readFileSync('README.md','utf8')"` — OK (task's verification command).
- `node -e "require('fs').readFileSync('CHANGELOG.md','utf8')"` — OK.
- Full suite (`node --test`, after `npm ci` to restore gitignored `mcp-server/node_modules/`): 178/178 pass, 0 fail.
- No build step, no lint config in this repo.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| README describes the three knowledge sources | pass |
| README describes the `spearhead-knowledge/` layout | pass |
| README describes the `search` MCP tool | pass |
| README states the `SPEARHEAD_EMBEDDINGS_API_KEY` requirement | pass |
| README describes opportunistic capture triggers (first-read, task-done) | pass |
| CHANGELOG gets a new version entry, consistent format/style | pass |

Every documented claim was cross-checked against the actual shipped implementation (`server.js`, `watch.js`, `embeddings.js`, `knowledge-nudge.js`, `remind.js`, `knowledge-path.js`), not just plausible-sounding prose.

**Informational flag (not a T-10 defect):** the new CHANGELOG entry is versioned `0.7.0` while `.claude-plugin/plugin.json`/`.kimi-plugin/plugin.json` remain at `0.6.0`. T-10's acceptance criteria only require README + CHANGELOG documentation, not a version bump, and this repo's convention is a separate `chore:` bump commit. Logged as a retro follow-up.

### Anti-reward-hacking checklist

- (a) no test deleted/skipped: pass — doc-only diff, no test files touched.
- (b) no assertion weakened vs PLAN.md testing strategy: pass — no test logic exists to weaken; the strategy's real requirement (accuracy against shipped implementation) is met.
- (c) no hardcoded expected output: pass — no tests; documented values are correct, not fabricated.
- (d) diff touches nothing out-of-scope: pass — confined to `README.md`, `CHANGELOG.md`, zero functional code.

**Overall: pass.**

## Merge and integration check

- `git merge --no-ff spearhead/T-10` into `main`.
- Full suite re-run on merged tree, excluding gitignored `spearhead-attacks/worktrees/*`.
- **Integration: pass.**
