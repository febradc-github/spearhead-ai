# V-3.1 — T-3: Content hashing + embeddings client module

## Mechanical gates (run for real, spearhead/T-3 branch, in worktree)

- `node mcp-server/lib/hash.test.js` — 5/5 pass.
- `node mcp-server/lib/embeddings.test.js` — 9/9 pass.
- Full pre-existing suite (unaffected):
  - `hooks/remind.test.js` — 15/15 pass.
  - `hooks/validate-state.test.js` — 9/9 pass.
  - `hooks/guard.test.js` — 14/14 pass.
  - `scripts/state.test.js` — 34/34 pass.
  - `lib/knowledge-frontmatter.test.js` — 12/12 pass.
  - `mcp-server/server.test.js` — 3/3 pass.
- Total: 101/101 pass, 0 fail.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| `hash.js` stable sha256 (`node:crypto`) of file content | pass |
| `embeddings.js` reads env vars, calls embeddings API via built-in `fetch`, no HTTP client dep | pass |
| Missing key / failed fetch throws a clear, named error | pass |
| Tests mock/inject `fetch`, no live network calls | pass |

### Anti-reward-hacking checklist

- (a) no test deleted/skipped: pass — all additions, 0 skipped/todo.
- (b) no assertion weakened vs PLAN.md testing strategy: pass — `node:test`/`node:assert/strict`, meaningful assertions incl. named-error-class rejection checks.
- (c) no hardcoded expected output: pass — the one literal (empty-string sha256) is the genuine, independently-confirmed constant, not a captured implementation output.
- (d) diff touches nothing out-of-scope: pass — confined to the four expected files under `mcp-server/lib/`.

**Overall: pass.**

## Merge and integration check

- `git merge --no-ff spearhead/T-3` into `main`.
- Full suite re-run on merged tree: 101/101 pass (remind 15, validate-state 9, guard 14, state 34, knowledge-frontmatter 12, mcp-server/server 3, hash 5, embeddings 9).
- **Integration: pass.**
