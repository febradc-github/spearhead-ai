# V-8.1 — T-8: Search-first reminder (extend remind.js + RULES.md)

## Mechanical gates (run for real, spearhead/T-8 branch, in worktree)

- `node hooks/remind.test.js` — 15/15 pass.
- Full pre-existing suite (unaffected):
  - `hooks/validate-state.test.js` — 9/9 pass.
  - `hooks/guard.test.js` — 14/14 pass.
  - `scripts/state.test.js` — 34/34 pass.
- Total: 72/72 pass, 0 fail.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| One line added to injected message, present in both full-rules and anchor variants, directing search-first before reading source | pass |
| `rules/RULES.md` updated with the same directive, `remind.js` stays byte-identical to it (existing test) | pass |
| No change to cadence logic (session id resolution, idle expiry, refresh interval) — text only | pass |

### Anti-reward-hacking checklist

- (a) no test deleted/skipped: pass — diff to `remind.test.js` purely additive (one new test), 0 skipped/todo.
- (b) no assertion weakened vs PLAN.md testing strategy: pass — byte-identical sync test unchanged and genuinely green; new test uses strict exact-phrase regex matching per variant.
- (c) no hardcoded expected output: pass — `fullMessage()` still reads RULES.md at runtime (single source of truth); anchor directive is legitimate literal code, not a test-satisfying shortcut.
- (d) diff touches nothing out-of-scope: pass — diff confined to exactly `hooks/remind.js`, `hooks/remind.test.js`, `rules/RULES.md`.

Known flag confirmed resolved: the coder's self-reported RULES.md line-wrap bug (directive split across two lines by markdown formatting) is fixed in the final diff — single unbroken line, byte-identical test genuinely passes.

**Overall: pass.**

## Merge and integration check

- `git merge --no-ff spearhead/T-8` into `main`.
- Full suite re-run on merged tree: 87/87 pass (remind 15, validate-state 9, guard 14, state 34, mcp-server 3, knowledge-frontmatter 12).
- **Integration: pass.**
