# V-4.1 — T-4: Index storage + cosine similarity

## Mechanical gates (run for real, spearhead/T-4 branch, in worktree)

- `node mcp-server/lib/index-store.test.js` — 10/10 pass.
- `node mcp-server/lib/similarity.test.js` — 12/12 pass.
- Full pre-existing suite (unaffected):
  - `hooks/remind.test.js` — 15/15 pass.
  - `hooks/validate-state.test.js` — 9/9 pass.
  - `hooks/guard.test.js` — 14/14 pass.
  - `scripts/state.test.js` — 34/34 pass.
  - `lib/knowledge-frontmatter.test.js` — 12/12 pass.
  - `mcp-server/server.test.js` — 3/3 pass.
- Total: 109/109 pass, 0 fail.

## Independent verdict (spearhead-verifier, fresh context)

| Criterion | Verdict |
|---|---|
| Reads/writes `spearhead-knowledge/index/embeddings.json` keyed by relative path, entries `{hash, embedding, updated, type}` | pass |
| Writes atomic (temp file + rename), never leaving a partial file | pass |
| Creates `spearhead-knowledge/index/` if missing | pass |
| `similarity.js` ranks by cosine similarity, returns top N `{path, score}` | pass |
| Tests use temp dir, no live embeddings API dependency | pass |

### Anti-reward-hacking checklist

- (a) no test deleted/skipped: pass — all additions, 0 skipped.
- (b) no assertion weakened vs PLAN.md testing strategy: pass — atomic-write claim backed by a genuine mid-write failure-injection test, not prose.
- (c) no hardcoded expected output: pass — cosine values are legitimate exact math for chosen unit vectors; top-8 test uses random embeddings, can't be gamed.
- (d) diff touches nothing out-of-scope: pass — confined to the four expected files.

**Overall: pass.**

## Merge and integration check

- `git merge --no-ff spearhead/T-4` into `main`.
- Full suite re-run on merged tree: 123/123 pass (remind 15, validate-state 9, guard 14, state 34, knowledge-frontmatter 12, mcp-server/server 3, hash 5, embeddings 9, index-store 10, similarity 12).
- **Integration: pass.**
